import { combineReducers } from 'react-redux';

const homePageReducer = (
  state = {},
  action,
) => {
  switch (action.type) {
    default:
      return { ...state };
  }
};

export default homePageReducer;
